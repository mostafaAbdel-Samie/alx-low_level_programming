#include "main.h"
/**
 * *_strstr - shek the pointer is foind 
 * @haystack: pointer char
 * @needle: pointer char
 */
char *_strstr(char *haystack, char *needle)
{
	int i, j;
	for (i = 0; i < haystach; i++)
	{
		for (j = 0; j < needle; j++)
		{
			if (haystach != '\0')
		}
	}
	return (NULL);
}
